{
  "files": [],
  "references": [ // all paths added here will produce declaration file in isolation, so we can take them as diferent modules
    {
      "path": "./tsconfig.node.json"
    },
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.vitest.json"
    }
  ]
}
